line-length = 140
target-version = "py314"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "B008",  # FastAPI's File(...)/Header(...)/Depends(...) call-as-default idiom
    "UP047", # PEP 695 generic syntax — not adopted elsewhere in this codebase
]
