fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.60
pydantic>=2.8
python-multipart>=0.0.9
python-dotenv>=1.0
Pillow>=10.4
pypdfium2==5.13.0  # PDF uploads: the first page rendered at config.PDF_RENDER_DPI; bundles pdfium, no system package
mrz>=0.6.2
httpx>=0.27
opencv-python-headless>=4.10
numpy>=2.0
# 180-degree orientation detection. Pinned: 1.4+ declares requires_python <3.13 and the
# engine image is python:3.14-slim. Installed --no-deps in the Dockerfile because it
# declares opencv-python while this engine uses opencv-python-headless — so the pin
# below is a comment only; a plain `pip install -r requirements.txt` must never pull
# rapidocr-onnxruntime in directly, or it drags opencv-python along with it. The real
# install is the --no-deps line in the Dockerfile.
# rapidocr-onnxruntime==1.2.3
onnxruntime>=1.17
pyclipper>=1.2.1
shapely>=1.7.1
PyYAML>=6.0
six>=1.15.0
